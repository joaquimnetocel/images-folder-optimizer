{
	"name": "images-folder-optimizer",
	"version": "0.0.26",
	"description": "This is a high speed package that uses Sharp.js to recurively transform, convert and optimize images from a folder to smaller, web-friendly JPEG, PNG, WebP, GIF and AVIF images of varying dimensions. It is also possible to add watermark to the transformed/optimized images.",
	"repository": {
		"type": "git",
		"url": "https://github.com/joaquimnetocel/images-folder-optimizer.git"
	},
	"bugs": {
		"url": "https://github.com/joaquimnetocel/images-folder-optimizer/issues"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"type": "module",
	"main": "dist/index.js",
	"types": "dist/index.d.ts",
	"scripts": {
		"format": "prettier --write .",
		"lint": "prettier --check . && eslint .",
		"build": "tsc",
		"example": "tsc && node dist/example.js",
		"npm-publish": "tsc && npm publish",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"devDependencies": {
		"@types/node": "^20.6.5",
		"@typescript-eslint/eslint-plugin": "^6.7.3",
		"@typescript-eslint/parser": "^6.7.3",
		"eslint": "^8.50.0",
		"prettier": "^3.0.3",
		"sharp": "^0.32.6",
		"typescript": "^5.2.2"
	},
	"files": [
		"/dist"
	]
}
